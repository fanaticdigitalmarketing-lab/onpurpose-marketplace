# ⚠️ LOCKED CONFIG — DO NOT CHANGE OR DELETE THIS FILE ⚠️
# Start command must be "node server.js" — do not revert to "npm start"
# Last verified working: Mar 31 2026

[phases.build]
cmds = ["npm install"]

[start]
cmd = "node server.js"
